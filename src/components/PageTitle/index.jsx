import styled from 'styled-components';

const PageTitle = styled.h1`
    font-size: 3rem; 
    font-weight: 900;
    margin-left: auto;
    margin-right: auto;
    color: #FFF;
`

export default PageTitle; 